
两个问题
1，居中问题，本例中我用的是flex2012的布局，优点在于代码简单，清晰明了，缺点是不兼容IE11以下浏览器
2，两个扇形问题，本例中我使用了border法，设置div的width和height为0，然后设置border半径为100px
之后再设置4个颜色为border的颜色，因为其中3个设置为透明色，所以并不能看到，最后使用transform对图形进行
旋转变换，再使用绝对定位固定到相应位置
